# Samples

This directory is reserved for lightweight sample artifacts only.

Examples:

- a tiny CSV manifest snippet
- a few handpicked metadata rows
- derived thumbnails only if redistribution is allowed

Do not commit the full public dataset into this repository.
